# ------------------------------------------------------------
# MERI 环境变量示例（不要把真实密钥提交到仓库）
# 1) 复制本文件为 .env
# 2) 填入自己的 Key / Endpoint
# ------------------------------------------------------------

# ============ 阿里云 DashScope（通义千问 OpenAI 兼容模式）===========
# 通义千问密钥（示例占位）
DASHSCOPE_API_KEY=your_dashscope_api_key

# ============ Azure OpenAI（GPT 系列）===========
# 本项目新增的一套变量（不会影响你现有的 DASHSCOPE_API_KEY）
#
# 你需要在 Azure Portal -> 你的 Azure OpenAI 资源 -> "Keys and Endpoint"
# 拿到：Key（Key 1/Key 2 任意一个）以及 Endpoint
AZURE_OPENAI_API_KEY=your_azure_openai_api_key
AZURE_OPENAI_ENDPOINT=https://your-resource-name.openai.azure.com

# Azure OpenAI REST 推理 API 版本（Microsoft Learn 文档显示 GA 为 2024-10-21）
# 如果你的资源/区域要求不同，可改成 portal/文档提示的 api-version
AZURE_OPENAI_API_VERSION=2024-10-21

# 模型（deployment）名称不作为环境变量强制要求：
# 在代码里把 model 写成 azure/<你的deployment名> 即可，例如：
#   model="azure/gpt-4o"
#   model="azure/gpt-4o-mini"

# ============ 兼容 LiteLLM 传统 Azure 变量名（可选）===========
# 如果你更习惯 LiteLLM 的环境变量命名，也可以用下面这套。
# 本项目会优先读 AZURE_OPENAI_*，其次读取 AZURE_API_*
#AZURE_API_KEY=your_azure_openai_api_key
#AZURE_API_BASE=https://your-resource-name.openai.azure.com
#AZURE_API_VERSION=2024-10-21

