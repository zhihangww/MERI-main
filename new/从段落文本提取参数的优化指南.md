# 从段落文本提取参数的优化指南

## ✅ 确认：MERI 支持从段落文本中提取参数

MERI 使用 **docling** 解析 PDF，会将文档中的所有内容（包括表格、段落、标题等）转换为结构化格式，然后发送给 GPT 模型进行提取。**因此，即使参数在段落文本中，MERI 也能正确提取。**

## 🔍 工作原理

1. **文档解析阶段**：docling 识别并提取：
   - 表格（Table）
   - 段落文本（Paragraph）
   - 标题（Title）
   - 章节标题（Section Header）
   - 列表（List）
   - 图片等

2. **格式转换阶段**：所有内容转换为 HTML 格式，保留位置信息（bbox）和页码

3. **提取阶段**：GPT 模型从所有内容中提取信息，不限于表格

## 📝 优化 Schema 的 Description

### ❌ 不推荐的写法（过于简单）

```json
"MAX_PRESSURE": {
    "label": "Maximum pressure",
    "description": "The maximum pressure.",
    "desiredUnit": "bar"
}
```

### ✅ 推荐的写法（针对段落文本）

```json
"MAX_PRESSURE": {
    "label": "Maximum pressure",
    "description": "The maximum operating pressure of the equipment. This information may appear in paragraph text, technical descriptions, or specification sections. Look for phrases like 'maximum pressure of X bar', 'max pressure: X', 'operating pressure up to X bar', or similar variations in the document text. Extract the numerical value and convert to bar if needed.",
    "desiredUnit": "bar"
}
```

## 🎯 优化技巧

### 1. **明确说明可能出现的格式**

在 description 中描述参数在段落中可能出现的表述方式：

```json
"description": "The flow rate capacity. In paragraph text, this may be written as 'flow rate of 100 m³/h', 'capacity: 100 m³/h', 'handles up to 100 m³/h', or 'rated flow 100 m³/h'. Extract the numerical value."
```

### 2. **说明可能的位置**

```json
"description": "The motor power rating. This information may be found in the technical specifications section, product description paragraphs, or performance characteristics text. Look for values expressed in kW or HP."
```

### 3. **处理多种表述方式**

```json
"description": "The operating temperature range. In the document, this might be described as 'operating temperature: -10°C to +50°C', 'temperature range from -10°C to +50°C', 'works in temperatures between -10°C and +50°C', or similar variations. Extract both minimum and maximum values if given as a range, or extract the single value if only one is specified."
```

### 4. **处理单位转换**

```json
"description": "The weight of the equipment. The value may be given in kg, g, or pounds in the paragraph text. Convert to kg as specified in desiredUnit. Look for phrases like 'weight: 50 kg', 'weighs 50 kg', 'mass 50 kg', etc."
```

### 5. **处理隐含信息**

```json
"description": "The warranty period. This may be explicitly stated as '3 years warranty' or 'warranty: 3 years', or it might be mentioned in text like 'comes with extended warranty of 3 years' or '3-year warranty included'. Extract the numerical value in years."
```

## 📊 对比示例

### 场景：从段落中提取"最大流量"

**段落文本示例：**
> "该泵的最大流量为 150 立方米每小时，适用于大型工业应用。在正常工作条件下，流量可达到 150 m³/h。"

**❌ 简单描述（可能提取不准确）：**
```json
"MAX_FLOW_RATE": {
    "label": "Maximum flow rate",
    "description": "The maximum flow rate.",
    "desiredUnit": "m³/h"
}
```

**✅ 详细描述（提取更准确）：**
```json
"MAX_FLOW_RATE": {
    "label": "Maximum flow rate",
    "description": "The maximum flow rate capacity of the pump, measured in m³/h. In paragraph text, this may be expressed as '最大流量为 X m³/h', 'maximum flow rate of X m³/h', 'flow rate: X m³/h', 'capacity X m³/h', or similar variations. Extract the numerical value and ensure the unit is m³/h.",
    "desiredUnit": "m³/h"
}
```

## ⚠️ 注意事项

1. **描述要具体但不过于限制**
   - ✅ 好："Look for values like 'pressure: X bar' or 'max pressure X bar'"
   - ❌ 不好："Only extract if written exactly as 'pressure: X bar'"

2. **考虑多种语言**
   - 如果文档是中文，在 description 中可以包含中文表述示例
   - 例如："可能表述为'最大压力为 X bar'或'maximum pressure X bar'"

3. **处理范围值**
   - 如果参数是范围（如温度范围），说明如何提取
   - 例如："Extract both minimum and maximum if given as a range"

4. **处理计算值**
   - 如果参数需要计算，在 description 中说明
   - 例如："If text says '3+4', extract value as 7"

## 🧪 测试建议

1. **先用一个参数测试**
   - 选择一个在段落中明确出现的参数
   - 测试提取效果

2. **逐步优化**
   - 如果提取不准确，细化 description
   - 添加更多可能的表述方式

3. **检查输出**
   - 查看 `output_result.json`
   - 检查 `text` 字段是否包含正确的原始文本
   - 检查 `value` 和 `unit` 是否正确

## 💡 实际案例

假设你要从以下段落中提取参数：

> "本设备的工作压力范围为 0.5 到 10 bar，最大工作压力为 10 bar。在正常工作条件下，压力可维持在 5-8 bar 之间。"

**优化的 Schema：**
```json
"WORKING_PRESSURE_RANGE": {
    "label": "Working pressure range",
    "description": "The working pressure range of the equipment, typically given as minimum to maximum values. In paragraph text, this may be written as '工作压力范围为 X 到 Y bar', 'pressure range: X to Y bar', 'operating pressure X-Y bar', or 'pressure between X and Y bar'. Extract both minimum and maximum values. If only one value is given, extract that value.",
    "desiredUnit": "bar"
},
"MAX_WORKING_PRESSURE": {
    "label": "Maximum working pressure",
    "description": "The maximum working pressure of the equipment. In paragraph text, look for phrases like '最大工作压力为 X bar', 'maximum working pressure X bar', 'max pressure: X bar', or 'maximum operating pressure of X bar'. Extract the numerical value.",
    "desiredUnit": "bar"
}
```

## 📌 总结

- ✅ MERI **支持**从段落文本中提取参数
- ✅ 优化 `description` 字段可以提高提取准确性
- ✅ 在 description 中描述参数在段落中可能出现的多种表述方式
- ✅ 测试并逐步优化 schema

